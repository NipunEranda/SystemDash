## Build Commands

# Compile builder

    gcc builder/build.c -o build

# Build for Apple Silicon: 

    ./build applesilicon

# Build for Apple Intel:

    ./build applex86
    
# Build for Linux:

    ./build linux

# Clean previous build

    ./build clean